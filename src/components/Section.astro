---
import Container from './Container.astro';

interface Props {
  dark?: boolean;
  id?: string;
}

const { dark = false, id } = Astro.props;
---

<section
  id={id}
  class={`py-24 max-[1000px]:py-12 ${dark ? 'bg-white/55' : ''}`}
>
  <Container>
    <slot />
  </Container>
</section>
